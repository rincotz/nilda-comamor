import React from 'react';
import { Link } from 'react-router-dom';

const NotFoundPage = () => (
    <div>Página não encontrada. <Link to='/'>Clique para voltar</Link></div>
);

export default NotFoundPage;
